# VisionDecode
Deep learning project for recognizing and reconstructing distorted text sequences from noisy grayscale images using CNN, BiLSTM, CTC Loss, and modern OCR techniques. Designed to handle blur, occlusion, overlapping characters, and visual artifacts.
